# 2026-09-18 — повторная сверка корневых замечаний (F-01…F-05), архивирование проектов Q-06/Q-07

1. **Проверка подозрения владельца:** работа по замечаниям была отработана не полностью. Статусы из старых сводок не совпадали с фактическим состоянием корпуса. Результаты сверки — `REVIEW-REGISTRY.md` §9.

2. **F-02 (Q-06/Q-07) — устранены устаревшие текущие статусы.** Оба решения уже в корпусе (`05` §4b, `03` §6a), карточки уже обновлены коммитом `90ac226`. Исправлены: вводный статус реестра решений `INDEX.md` §4, таблица ID `AGENTS.md` §3, таблица ID скилла, уведомление `NEXT-SESSION-PROMPT.md`. Исторические записи не редактировались.

3. **Архивирование.** `OPEN-QUESTIONS-Q-06-Q-07.md` перенесён в `корзина/` байтово без изменений (SHA256 `f194d0951464a167e414f9b26472ec20807419961e0690559de6de31043754f9`); 11 ссылок обновлены в 4 файлах (`INDEX`, `CHANGELOG`, `REVIEW-REGISTRY`, `NEXT-SESSION-PROMPT`). Аудит `pre-task-audit-sonnet5.md`, журнал `pre-task-audit-sonnet5-fixes.md` и `NEXT-SESSION-PROMPT.md` оставлены в корне — F-05 не завершён. Содержимое архива не читалось.

4. **Проверено и не исправлено (открытые остатки):**
   - **F-04 — редакционный остаток:** заголовок `INDEX.md` всё ещё v2.6; якорь в `REVIEW-REGISTRY.md` v2.5; `ARCHITECTURE.md` и `corpus-map.md` — v2.5. Требуется синхронизация служебных метаданных с v2.7.
   - **F-05 — материализация ADR:** решения владельца нет; отдельные файлы ADR отсутствуют.
   - **Остаток S-09:** манифест хешей и реальный `BASELINE_ID` не созданы — соответствует решению реестра §4 «только норма, без создания инфраструктуры», объявлять выполненным нельзя.
   - `canonical-table.md` отстаёт от индекса (нет A-22…A-25 и Q-09…Q-13, устаревшие статусы Q-08/N-01/N-02/D-4).
   - `05`: два соседних одинаковых заголовка §5 «Принцип атомарности инструментов (Tool Granularity)».
   - Карточки Astra1: остались старые блокировки по уже принятым решениям (Q-09/Q-11/Q-13) — карточки в этой сессии не менялись.
   - Неотслеживаемые файлы аудита и `PreTaskAudit-ТЗv2.4-claude5.zip` — судьба не определена владельцем.

5. **Проверки:** `validate_index.py` → `PASS — предупреждений 0`; `test_validate_index.py` — 6 тестов OK. Правки — только служебные и реестровые, нормативные файлы 01–07 и приложения не менялись. Норма не создавалась, ID не менялись. Push не выполнялся — отправка за владельцем (`push.bat`).
