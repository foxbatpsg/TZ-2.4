# 2026-09-13

## Реестр замечаний REVIEW-REGISTRY.md

Создан `REVIEW-REGISTRY.md` — единый реестр решений по 20 замечаниям к ТЗ v2.4 (17 из `SUGGESTIONS.md` + 3 новых из `замечания GPT6.md`).

**Baseline:** `TZ-2.4 @ 1a26485`, ветка master.

**Вердикты:** ACCEPT — 15, ACCEPT-MODIFIED — 2, REJECT-VERIFIED — 3.

**P0-блокеры (подтверждены проверкой по тексту):**
- N-01 — MCP-host/владелец агентного цикла не определён (05 §2–3, противоречие ARCHITECTURE §1.4 п.1 vs §1.2/§4.1).
- S-12 / Q-08 — A-21 (новая версия Document при смене URL) нереализуем при `content_hash UNIQUE` (03 §9, стр. 277/291). ТЗ само признаёт открытым: 07 §17 стр. 249.
- N-02 — SourceRelation: UNIQUE по `(study_id, source_source_id, target_source_id)` не учитывает `claim_id`; нет FK на Claim. 03 §8a стр. 239/250. Конфликт с Q-02 (01 §7 стр. 341).

**Отклонено с пометкой REJECT-VERIFIED (дефекты выгрузки ревьюера, не ТЗ):**
- S-05 — «EPIC-07…11 недоступны» — неверно, они есть: EPIC SPECIFICATIONS стр. 168–245, MASTER ROADMAP стр. 341–442.
- S-01 — «нужен новый EPIC упаковки» — EPIC-11 PACKAGING/PORTABLE уже существует (стр. 234–244); уточнять внутри него.
- S-02 — «нет ModelManager» — Health Monitor + LLM Backend Router уже есть (05 §1–2, EPIC-06); нужен только preflight.

**Важная попутная находка:** `EPIC SPECIFICATIONS v1.0 — BASELINE.md` стр. 228 (EPIC-10 Coverage) содержит «Russian morphology если включена», что противоречит обязательности RU-морфологии по A-13. Зафиксировано как D-1.

**Решение пользователя:** отказался от навигационного слоя (INDEX/TRACEABILITY/BASELINE/ADR) и патч-плана — нужен только реестр.

**Полный список A-xx:** A-01…A-21. **Полный список Q-xx:** Q-01, Q-02, Q-03, Q-04, Q-05, Q-08.
**Gate:** G-01…G-11, определения в EPIC SPECIFICATIONS стр. 26–244.

## Финальное решение по навигации (изменено)

Пользователь выбрал: **Вариант 1 (индексный слой), спроектированный под последующий переход к Варианту 2** (машиночитаемые требования).

**Объём:** основное ТЗ 01–07 + приложения к ТЗ (7 файлов). **Карточки Astra1 и GPT6 в схему НЕ включаются** (решение пользователя) — это производные инструкции, не нормы.

**Критически важная находка:** система ID норм **уже существует в самом ТЗ** — A-01…A-21 и Q-01…Q-05 определены прямо в тексте в формате `Название нормы (A-N): текст`. Свои ID изобретать НЕ нужно. Это снимает главный риск варианта 2.

**Структура requirements.yaml** (задел под вариант 2):
`id, canonical (первичный источник), title, text, mentions[], conflicts[], gates[], epics[], status, verdict`

## ✅ СОЗДАН INDEX.md — навигационный слой

**Файл:** `INDEX.md` в корне проекта (~25 КБ, 10 разделов).

**Структура:**
1. Как пользоваться индексом (таблица «вопрос → раздел»)
2. Карта разделов основного ТЗ 01–07 (с ключевым содержанием каждого §)
3. **Реестр норм A-01…A-21** — колонки: ID, название, canonical, mentions, связь с реестром
4. Реестр решений Q-01…Q-08
5. Карта сущностей данных (~25 сущностей с разделом, правилами, связями)
6. Карта Gate G-01…G-11 (привязка к EPIC + критерий)
7. Карта EPIC-01…EPIC-11 (Gate + файлы модулей + нормативные приложения)
8. Карта механизмов (сквозной поиск: механизм → раздел ТЗ → приложение → реестр)
9. Открытые дефекты корпуса (D-1…D-4 + 3 P0)
10. Задел под вариант 2 — готовые примеры YAML-записей

**Все якоря проверены построчно** (A-05 стр.425, A-08 стр.293, A-13 стр.284, Q-01 стр.816, Q-05 стр.115 — совпали).

**Новые находки при построении:**
- **D-2:** Q-06 и Q-07 отсутствуют в корпусе — пропуск нумерации без пояснения (по аналогии с MASTER ROADMAP стр.244 «пропуск ≠ отсутствие требования»).
- **D-4:** масштаб дублей определений больше, чем казалось: A-03 — **7 определений**, A-02/A-10/A-21/Q-04 — по 4. При переходе к варианту 2 поле `canonical` обязательно.

**Canonical для A-xx (зафиксировано в индексе):** A-02→`01`§2.3, A-03→`03`§2, A-04→`03`§6, A-05→`03`§13, A-06→`03`§11, A-07→`03`§11, A-08→`03`§9a, A-09→`03`§25.2, A-10→`03`§15, A-11→`03`§24, A-12→`02`§6a, A-13→`04`§14.1, A-14→`04`§28, A-15→`01`§7, A-16→`04`§26, A-17→`02`стр.100, A-18→`03`§17b, A-19→`05`§16, A-20→`05`§3.1, A-21→`03`§9, Q-01→`03`§25.3, Q-02→`01`§7, Q-03→`04`§22, Q-04→`03`§17c, Q-05→`01`§2.3, Q-08→`07`§17.

## ✅ СОЗДАН СКИЛЛ tz-index-maintenance

**Путь:** `.workbuddy-ai/skills/tz-index-maintenance/` (проектный скилл, не пользовательский — это специфика корпуса ТЗ).

**Структура:**
- `SKILL.md` — рабочий процесс: найти норму через индекс → проверить `canonical` и связи → внести правку по 6 правилам → обязательно прогнать валидатор. Плюс разбор типовых ситуаций (добавление нормы, пропуск нумерации, норма без определения, написание TASK-карточки) и границы применимости.
- `references/corpus-map.md` — карта 14 файлов корпуса: назначение, область ответственности, где какая категория норм живёт, иерархия приоритетов.
- `references/canonical-table.md` — таблица «норма → первичный источник» для всех A-xx и Q-xx + список дефектов.
- `scripts/validate_index.py` — рабочий валидатор (проверен, даёт PASS).

**Что делает валидатор (6 проверок):** разрешимость ссылок на файлы в INDEX.md, полнота реестра норм (все ли A-xx/Q-xx из корпуса попали в индекс), обратная полнота (нет ли фантомных записей), дубли определений, нормы без канонического определения, покрытие Gate G-01…G-11.

**Особенность:** константа `ALLOWED_GAPS = {"Q-06", "Q-07"}` легитимизирует задокументированный пропуск нумерации — иначе валидатор падал бы на Q-06/Q-07.

**6 обязательных правил правки (в SKILL.md):** сохранять формат `Название (A-N): текст`; не менять нормативные ID; обновлять INDEX.md тем же изменением; не дублировать текст нормы в индексе; не разрешать противоречия самостоятельно; P0-блокеры — только после ADR.

**Важная находка валидатора:** реальный масштаб дублей больше, чем при ручном анализе. **A-03 определён 9 раз**, A-11 и A-16 — по 5, ещё 18 норм — от 2 до 4. Это усиливает требование обязательного поля `canonical` при переходе к варианту 2.

**Q-08 не имеет определения** — только упоминается как открытый в `07` §17 стр.249. Отражено в индексе пометкой «⟂ собственного определения нет».

**В INDEX.md добавлен §0 «Протокол работы с индексом»** — предписывает загружать скилл перед правкой корпуса и прогонять валидатор перед завершением.

**Итоговое состояние файлов:**
- `INDEX.md` (31 КБ) — 11 разделов (0–10)
- `REVIEW-REGISTRY.md` (30 КБ) — реестр 20 замечаний + D-1…D-4
- `.workbuddy-ai/skills/tz-index-maintenance/` — 4 файла

**Осталось незакрытым:** A-01 (отложенный вопрос, вернуться и попытаться восстановить формулировку из 5 применений); 3 P0-блокера (N-01, S-12, N-02) ждут ADR.

## ✅ A-01 РАЗРЕШЁН — формулировка найдена в старых файлах

**Пользователь нашёл каноническое определение A-01 в старых файлах проекта:**

> **A-01. Редакционные ошибки Roadmap и граф зависимостей**
> *Источник: Roadmap, заголовок, §7, §15, Change Log.*
> **Решение:**
> - Версия Roadmap в шапке: 1.2, а не 1.1.
> - Пропуски пунктов 8 и 10 в Search Core не означают отсутствующие требования. Карточки привязаны к названиям подэтапов, а не к ошибочной нумерации.
> - Удалить техническое ребро EPIC-05 → EPIC-06: Change Log и строка зависимостей EPIC-06 явно разрешают независимый backend.
> - Последовательность выдачи эпиков при этом остаётся gated.
> **Проверка:** граф ацикличен; заявленные зависимости и обратные связи согласованы.

**Диагноз уточнён: это НЕ утрата данных, а ДЕФЕКТ МИГРАЦИИ.** Правило было свёрнуто в `MASTER ROADMAP v1.2` Change Log пункт 12 (стр. 575), при этом потеряны 4 составные части:
- последовательность остаётся gated (частично сохранилась в EPIC SPEC стр.146)
- **источник правила** («Roadmap, §7, §15, Change Log»)
- **обоснование** («Change Log и строка зависимостей EPIC-06 разрешают независимый backend»)
- **привязка карточек к названиям подэтапов, не к нумерации**
- **критерий приёмки** («граф ацикличен; зависимости и обратные связи согласованы»)

**Ключевой вывод:** без обоснования и критерия приёмки норма становится непроверяемой — видно «что сделано», но не видно «почему» и «как убедиться».

**Что обновлено:** REVIEW-REGISTRY.md §D-4 → `RESOLVED` с полным текстом и таблицей потерь; сводная таблица → `RESOLVED` (итог: ACCEPT 15, ACCEPT-MODIFIED 2, REJECT-VERIFIED 3, RESOLVED 1); INDEX.md — A-01 в реестре норм, §9 D-3/D-4, YAML-пример (`status: active`); canonical-table.md; SKILL.md — добавлен **рабочий приём «поиск нормы в старых файлах проекта»**.

**Новый приём в скилле (важно для будущего):** при норме без определения — сначала искать формулировку в старых файлах проекта по названию/теме/фрагментам применений, а не сразу классифицировать как утрату. Проверять у найденной нормы наличие трёх частей: источник, обоснование, критерий приёмки.

**Правка в корпус НЕ внесена** — восстановление нормативного текста требует утверждения. Формулировка предложена дословно.

Валидация после всех правок: **PASS**.
Поле `canonical` решает проблему дублей определений (A-03 определён 7 раз, A-02/A-10/A-21/Q-04 — по 4 раза); поле `conflicts` — проблему рассинхрона норм; поле `verdict` — связь с REVIEW-REGISTRY.md.

### ⚑ ОТЛОЖЕННЫЙ ВОПРОС: A-01 — ПРОВЕРИТЬ ПОСЛЕ ПОСТРОЕНИЯ СХЕМЫ

**A-01 не имеет канонического определения** ни в одном из 7 разделов ТЗ, ни в приложениях. Существуют только 5 разрозненных примечаний-применений:
- `EPIC SPECIFICATIONS` стр. 146 — нет архитектурной зависимости EPIC-06 от EPIC-05
- `MASTER ROADMAP` стр. 6 — версия трактуется как 1.2
- `MASTER ROADMAP` стр. 244 — пропуски номеров подэтапов 8/10 в EPIC-04 не являются отсутствующими требованиями
- `MASTER ROADMAP` стр. 575 — ребро EPIC-05 → EPIC-06 в Dependency Matrix удалено
- `EPIC → TASK DECOMPOSITION` стр. 5 — A-01 входит в состав «Утверждённых изменений v1.0»

**Вердикт пользователя (предварительный):** это **ошибка при ревизиях**, не задуманная группа норм. A-01 входит в нормативный набор «УИ v1.0 (A-01…A-21, Q-01…Q-05)», цитируемый в шапках трёх приложений, но собственной формулировки не имеет.

**ЗАДАЧА НА ПОСЛЕ СХЕМЫ:** вернуться к A-01, проверить, восстанавливается ли его каноническая формулировка из контекста применений. Если да — предложить формулировку на утверждение. Если нет — внести в реестр как **дефект ревизии** наравне с D-1 (A-13 vs EPIC-10). Не изобретать определение самостоятельно.

---

## ИТОГ СЕССИИ 2026-09-13 (состояние перед закрытием)

**Коммит `3ada8b2` «создали индекс»** зафиксировал версию ДО разрешения A-01. В рабочем дереве (не закоммичено) лежит правка A-01 → `RESOLVED`.

`git status --short`:
```
 M .workbuddy-ai/memory/2026-09-13.md
 M INDEX.md
 M REVIEW-REGISTRY.md
?? .workbuddy-ai/skills/
```
`git diff --stat`: 3 файла, +136 / −21.

**Артефакты на диске:**
| Файл | Размер | Статус |
|---|---|---|
| `INDEX.md` | 32 680 B | изменён, не закоммичен |
| `REVIEW-REGISTRY.md` | 33 818 B | изменён, не закоммичен |
| `.workbuddy-ai/skills/tz-index-maintenance/SKILL.md` | — | новый, не в git |
| `.workbuddy-ai/skills/tz-index-maintenance/references/canonical-table.md` | — | новый |
| `.workbuddy-ai/skills/tz-index-maintenance/references/corpus-map.md` | — | новый |
| `.workbuddy-ai/skills/tz-index-maintenance/scripts/validate_index.py` | — | новый |

**Важно:** `.workbuddy-ai/` НЕ в `.gitignore` → папка скилла попадёт в коммит при `git add`. В `.gitignore` только: `git-push.bat`, `корзина/`, `.vscode/`, `.idea/`, `.env*`, ОС-мусор. `git-push.bat` в корне проекта — штатный способ отправки.

**Проверка A-01 в рабочем дереве (подтверждена):**
- `INDEX.md:78` — `A-01 | Редакционные ошибки Roadmap и граф зависимостей | ⟂ восстановлено из старых файлов | D-4 (RESOLVED)`
- `INDEX.md:243` — D-3 → RESOLVED
- `REVIEW-REGISTRY.md:44, 80` — D-4 → RESOLVED
- `canonical-table.md:11` — A-01 заполнен

**План следующей сессии (по договорённости с пользователем):**
1. Обновить сессию → **проверить, срабатывает ли Skill `tz-index-maintenance`** (автозагрузка по триггеру).
2. В той же сессии — **внести правки в корпус ТЗ** (то, ради чего скилл написан).
3. Открыто: 3 P0-блокера (N-01, S-12/Q-08, N-02) ждут ADR; вопрос внесения A-01 в нормативный состав «УИ v1.0».
4. ~~Требует подтверждения: коммитить ли текущие изменения + папку скилла.~~ **РЕШЕНО пользователем: коммит сделан, скилл сохраняется в git.**

**Коммит `4694f24` «A-01 восстановлен + skill сопровождения индекса»** — 7 файлов, +721 / −21. Рабочее дерево чистое.

Проверено в HEAD:
- `.workbuddy-ai/skills/tz-index-maintenance/` — 4 файла в git (SKILL.md, canonical-table.md, corpus-map.md, validate_index.py)
- `INDEX.md` — 4 вхождения `RESOLVED`, строка 74 «A-01 восстановлен»
- `SKILL.md` frontmatter: `name: tz-index-maintenance`, `agent_created: true`, описание с перечнем триггеров

**Соглашение по git:** папка `.workbuddy-ai/skills/` версионируется вместе с проектом (не в `.gitignore`) — скиллы уровня проекта едут в репозиторий. Отправка — через `git-push.bat` в корне.

---

## КРОСС-СРЕДОВАЯ ПЕРЕНОСИМОСТЬ: `AGENTS.md`

**Проблема:** скилл `tz-index-maintenance` — формат WorkBuddy (`.workbuddy-ai/skills/` + `agent_created: true`). Cline и OpenCode его **не прочитают** — у них другие пути: Cline → `.clinerules/`, OpenCode → `AGENTS.md` в корне.

**Решение (Вариант A, выбран пользователем):** создан **`AGENTS.md` в корне проекта** — универсальная точка входа, которую читают OpenCode, Cline, Cursor, WorkBuddy и большинство агентских сред.

**`AGENTS.md` содержит:**
- §1 зачем проект и почему нельзя читать корпус целиком (кейс EPIC-07…11)
- §2 карта корпуса: нормативное ядро / приложения / навигация / производное + иерархия приоритетов
- §3 система ID норм (`A-xx`, `Q-xx`, `G-xx`) и обязательный формат определения
- §4 рабочий процесс 4 шага (поиск в INDEX → canonical → 6 правил правки → валидация)
- §5 типовые ситуации: новая норма, пропуск нумерации, норма без определения (приём «искать в старых файлах»), TASK-карточка
- §6 границы: не читать `корзина/`, не решать за человека, не перенумеровывать, не изобретать формулировки
- §7 справочные материалы, §8 шпаргалка

**Ключевое дополнение к скиллу — раздел про интерпретатор Python:**
Скрипт среда-независим (только stdlib, Python 3.8+). Прописаны фолбэки:
```bash
python3 .../validate_index.py    # Linux/macOS
py -3   .../validate_index.py    # Windows launcher
python  .../validate_index.py    # общий случай
```
Плюс явный флаг `--root "<путь>"` для запуска из другого каталога.

**Стратегия версионирования (принята):**
- `AGENTS.md` — источник истины для правок корпуса в любой среде
- `.workbuddy-ai/skills/.../SKILL.md` — детальная версия для WorkBuddy (остаётся)
- `references/*.md` и `scripts/validate_index.py` — обычные файлы, читаются из любой среды по прямому пути

**Проверено:** валидатор после создания `AGENTS.md` → `РЕЗУЛЬТАТ: PASS` (те же 2 предупреждения: 21 норма с дублями определений, `Q-08` без определения — оба задокументированы в §9 индекса).
