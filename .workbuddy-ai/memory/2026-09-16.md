# 2026-09-16 — отработка находок независимого аудита (F-01…F-05)

**Контекст:** аудит `pre-task-audit-sonnet5.md` (5 находок). Решение владельца по объёму: `F-03` не исправляем, остальные — по одному. Ничего, не относящегося к находкам, не менялось.

## Сделано

1. **`F-01` — карта MCP-инструментов (`ARCHITECTURE.md`).** В дереве `mcp_server/tools/` и в таблице §4.1 было 6 групп / 17 инструментов при тексте «23 инструмента». Добавлены `cache_diagnostics.py`, `report.py`, `job_management.py` и три группы. Проверка машинной сверкой с `MCP TOOL CONTRACTS v1.1` §10: 23 = 23 = 23, состав и порядок совпадают.
2. **`F-04` — ревизия версии и журнал (решение владельца — вариант «всё согласовано»).**
   - версия `v2.4` → `v2.5` во всех 16 документах корпуса, где указана версия ТЗ (25 упоминаний): 7 разделов ТЗ, 5 нормативных приложений, 4 служебных файла. Версии самих приложений (`v1.0`/`v1.1`/`v1.2`) не изменялись;
   - создан `CHANGELOG.md` — первая запись (ревизия v2.5) со сведениями из `REVIEW-REGISTRY.md`: `ADR-001/002/003`, `N-01…N-03`, `S-01…S-17` с вердиктами, `D-4`, `D-6`, `Q-09…Q-13`, сводка вердиктов и открытые пункты;
   - `Baseline` обновлён: `TZ-2.5 @ 32e115f` (коммит с правками ревизии); предыдущий снимок `TZ-2.4 @ 1a26485` сохранён в записи реестра.
3. **`F-03` — не исправлялся** (осознанное решение архитектора оставить `корзина/` вне git-истории).

Правки внесены двумя коммитами: `32e115f` (корпус + `CHANGELOG.md`) и коммит обновления якоря. Ничего не пушилось — отправка за владельцем.

## Проверки

- `validate_index.py` → `РЕЗУЛЬТАТ: PASS — предупреждений 0` после каждого шага.
- Diff минимальный: 25 замен по одной строке + правка `F-01` + 2 строки якоря; переводы строк не поехали (часть файлов CRLF — правка только через `newline=''`).
- Остатки старой версии вне корпуса (осознанно оставлены): имя репозитория `foxbatpsg/TZ-2.4` в `ARCHITECTURE.md` стр. 3, реальный путь к папке в примере команды `AGENTS.md` стр. 184, исторические упоминания в журнале изменений и в файлах аудита.

## Техническое замечание (инструментальное)

Инструмент редактирования дважды отчитался об успешной правке строки `Baseline`, но на диск правки не попали (обнаружено сверкой `mtime` и содержимого). Правка выполнена скриптом с контролем `count(old) == 1`. **Правило на будущее: после любой правки корпуса — контрольное чтение фрагмента с диска, а не доверие отчёту инструмента.**

## Открытые вопросы

1. **Служебные файлы рабочего контура** (`.workbuddy-ai/`): в памятке проекта, инструкции для агентов, карте корпуса и описании скрипта-валидатора указана версия `v2.4` и старый якорь. Обновлять ли их до `v2.5`? (Задан вопросом владельцу 2026-09-16.)
2. **`F-02`** — причина пропуска номеров `Q-06`/`Q-07` не документирована в корпусе; нужна формулировка владельца либо разрешение прочитать архивные файлы из `корзина/`. Дефект `D-2` в индексе остаётся открытым.
3. **`F-04` остаток** — норма «согласованный baseline документов» требует машинночитаемого набора (пути/версии/**хеши**) и реального идентификатора baseline в карточках задач; введено только навигационное значение `Baseline`, в задачах — переходное `PRE-BASELINE`.
4. **`F-05`** — материализация `ADR-001/002/003` отдельными файлами: решение владельца не принято.
5. **Внешние идентификаторы** — имя рабочей папки `ТЗ 2.4`, имя репозитория `foxbatpsg/TZ-2.4`, локальные скрипты отправки (`push.bat`/`git-push.bat` с текстом «ТЗ 2.4») не изменялись: это не версия документа.