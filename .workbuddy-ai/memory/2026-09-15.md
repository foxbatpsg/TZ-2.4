# 2026-09-15 — Внесение правок A-22…A-25 (модель, схема, движок, лемматизация)

**Основание:** план «план правок — A-22…A-25 (модель, схема, движок, лемматизация).md», решения владельца от 2026-09-15. Внесено **дословно по плану**, отдельными коммитами, валидатор после каждой части.

## Внесено (5 коммитов)

| Коммит | Что |
|---|---|
| `c30bd3a` | chore: INDEX.md — снята ссылка на удалённый владельцем файл `замечания GPT6.md` (стр. 12, колонка «Не включено»). Решение владельца в этой сессии: файл удалён намеренно. До этого валидатор падал на проверке [1] (ссылки на файлы) ещё на HEAD — это была единственная причина FAIL, не связанная с A-22…A-25 |
| `1670cd4` | **A-22** — минимально поддерживаемый класс LLM: 12–14B Q4/Q5 (GGUF); верхней границы нет. Норма — `02` §5.3 (стр. 155). Замены: `02` §5.1 стр.134, `02` §6 стр.161, `05` §1 стр.13, `07` §25 стр.393, `ARCHITECTURE.md` стр.40. INDEX §3 — строка A-22 |
| `953dbc9` | **A-23** — embedding-ready схема: боковая таблица `ChunkEmbedding(chunk_id, model_id, vector, created_at)` рядом с `DocumentChunk`, append-only миграция (S-17). Норма — `03` §11 (стр. 374, после абзаца о числовых сигнатурах A-03). Дополнение `02` §5.2 стр.138. INDEX §3 (A-23) + §5 (строка `ChunkEmbedding`, «зарезервировано, вне MVP») |
| `a3c8edc` | **A-24** — хранимый BM25-индекс: SQLite FTS5, персистентный в Project DB; пересборка при каждом запуске недопустима. Норма — `04` §14 (стр. 257, перед §14.1). Замена `02` §5.1 стр.133. INDEX §3 (A-24) + §8 (строка «BM25 и RU-морфология» + A-24) |
| `df7b1d8` | **A-25** — словарная лемматизация (pymorphy3 или совместимый) как нормативная реализация A-13; стемминг — только явно логируемая деградация (A-13/D-1); кэш лемм обязателен; словарь в portable-сборке (EPIC-11). Норма — `04` §14.1 (стр. 290, после нормы A-13). INDEX §3 (A-25) + §8 (+A-25) |

## Особенности и уроки

1. **Перекрёстная ссылка A-24→A-25 ломает промежуточную валидацию.** Норма A-24 (дословно по плану) содержит «(A-25)» — после коммита 3 валидатор давал FAIL «нормы есть в корпусе, но отсутствуют в INDEX.md: A-25» до внесения A-25 в Части 4. Это свойство плана, не дефект исполнения: A-24 и A-25 ссылаются друг на друга, поэтому промежуточный PASS между коммитами 3 и 4 недостижим в принципе. После коммита 4 — PASS.
2. **Строки-адреса в INDEX уточнялись контрольным чтением** после вставок: A-24 — стр. 257 (не 256), A-25 — стр. 290 (не 288), `02` §6 — стр. 161 (не 160).
3. Предупреждения валидатора (21 норма с несколькими определениями; Q-08/Q-10/Q-12/Q-13 без канонического определения) — ранее известные, не связаны с этой сессией.

## Что правки НЕ меняют
FSM, MCP-контракты, бюджетную модель, контур безопасности (N-03/Q-10), базовую схему данных. Ретривал-бенчмарк остаётся приёмочным инструментом G-04 (S-15); бенчмарк-церемонии выбора морфологии/движка и GUI-замеры отменены владельцем. Benchmark-spike Q-12 сохранён: два профиля (CPU-baseline, GPU-extended), результаты → G-08/G-10/G-11.

## Валидация
`validate_index.py` → **РЕЗУЛЬТАТ: PASS** (36 норм, фантомов нет, ссылки разрешаются, Gate G-01…G-11 покрыты). После коммита 4 — чистый PASS.

## Чистка остатков (та же сессия)

## Доработка реестров: D-6 (формат определений Q-норм) + аудит D-4 (та же сессия)

**Основание:** вопросы владельца: (3) «почистим остатки, отработанные файлы — в корзину, чистый коммит»; (4) «может, это стоит отрегулировать? зачем нам разночтения?» — про два предупреждения валидатора.

**Пункт 3 — выполнен коммитом `6c54471`:** `prompt.txt` и `план правок — A-22…A-25….md` перемещены в `корзина/` (в `.gitignore`, из репозитория ушли); удаления `замечания GPT6.md` и `OPEN-QUESTIONS-for-Architector.md` зафиксированы там же. Дерево чистое, `master` = `origin/master`.

**Пункт 4 — разбор «разночтений» разделён на два разных класса:**

| Класс | Что это | Факт | Действие |
|---|---|---|---|
| [4] «21 норма с несколькими определениями» | это **D-4** — давно известный дефект реестра | аудит всех 21 нормы: **противоречий нет**, все доп. вхождения — адресные пересказы нормы под локальным подзаголовком (A-11: «Гарантии durability» / «Мониторинг» / «durability-режим»; A-20: «Runtime stdout protection» / «CI policy»; A-04: «Область уникальности» / «Меж-Study изоляция» / «Область владения»). `canonical` заполнен для всех `A-01…A-25`, `Q-01…Q-13` | D-4 в INDEX §9 переведён в статус «АУДИТ 2026-09-15: противоречий не обнаружено»; устаревшие счётчики (A-03 ×7 → ×9) исправлены. Унификация локальных подзаголовков — отдельным решением, **не P0**, намеренно не делалась |
| [5] «Q-08/Q-10/Q-12/Q-13 без определения» | **новый дефект D-6** — дефект ФОРМЫ, не утрата нормы | нормативный текст есть и закрыт, но не по шаблону: заголовок `### 3.3. … (Q-10)` (нет `:` сразу после `(Q-N)`), `**Исключено из MVP (`Q-13`):**` (ID в бэктиках). Валидатор `REQ_DEFINITION = [«\w].{2,90}?\(([AQ]-\d{2})\)\s*:` требует двоеточия сразу после скобки | ✅ **CLOSED 2026-09-15** — приведено к шаблону в 4 файлах (см. ниже) |

**Внесено (по одной строке в каждом файле, без изменения смысла):**
1. `03` §9b — `**Механизм версионирования Document (Q-08):** …` (заголовок сокращён до `(Q-08)`, ADR-002 перенесён в текст нормы);
2. `05` §3.3 — `**Query egress policy (Q-10):** …`;
3. `02` §5.3 — `**Целевая среда и performance budget (Q-12):** …` (формулировка TARGET сохранена дословно);
4. `EPIC SPECIFICATIONS v1.0` §12 — `**Административный диагностический доступ (Q-13):** …` (метка «Исключено из MVP» заменена на название нормы; близнец названия уже был в `02` §7 стр.301).

**Эталон формата в корпусе уже существовал:** `**Политика верификации авторов и статистики (Q-09):**` (`04` §16a), `**Перенос Study и откат (Q-11):**` (`06` §13) — нормы, принятые 2026-09-15. Четыре нормы просто не привели к тому же виду.

**Трассировка:** `INDEX.md` §3 (счётчики + вывод аудита), §4 (заметка о формате + явный canonical у `Q-13`), §9 (`D-4` обновлён, `D-6` добавлен), `REVIEW-REGISTRY.md` (сводка, «Итог» 4→5, новый раздел D-6, пометка о нумерации: `D-5` живёт только в INDEX §9).

**Урок:** класс дефекта «норма есть, но не распознаётся» отделяется от «нормы нет». Проверка — по регулярному выражению валидатора, а не по смыслу: определение = `Название (ID): текст` с двоеточием сразу после скобки.

## Закрытие D-4: унификация определений норм (та же сессия)

**Основание:** решение владельца — «делай по плану, не будем оставлять хвосты». Закрыт класс дефекта `D-4` (дубли определений норм) — то, что в предыдущей сессии было отложено как «не P0».

**Что сделано (порядок важен):**
1. **Инвентаризация (скриптом, не чтением).** Найдено **74 вхождения** формы `Название (A-N): текст` на 21 норму — включая составные ссылки вида `(A-05/A-06)`, которые прежние сканы пропускали. Валидатор (`REQ_DEFINITION = [«\w].{2,90}?\(([AQ]-\d{2})\)\s*:`) ловит их как определения, поэтому «шумный» класс [4] держался на них.
2. **Выбор «хранителя» — по факту, а не произвольно.** Для каждой нормы оставлено то вхождение, на которое указывает поле `canonical` в INDEX §3/§4. Расхождения обнаружились у **20 норм**: `canonical` указывал на строку, определением не являющуюся (например `A-05` → `03` §13 стр. 425 — заголовок «Правило уникальности Evidence» без двоеточия).
3. **Фаза A (53 правки):** неканонические вхождения переведены в ссылочную форму `` (`A-N`) ``. Стиль не изобретался — эталон уже был в корпусе (`07` §28.1 стр. 575 «(`A-13`)»). Тексты норм не редактировались — менялся только маркер `(A-N):` → `` (`A-N`): ``.
4. **Фаза B (5 правок):** у норм, чья «каноническая» строка не распознавалась, маркер нормы вставлен в её первичный источник: `A-03` → `03` §2 («Критическое правило реляционной целостности»), `A-05` → `03` §13, `A-08` → `03` §9a, `Q-01` → `03` §25.3, `Q-02` → `01` §7.
5. **Фаза C:** адреса `canonical` и `mentions` приведены к факту (47 + 7 замен), §9 `D-4` → `CLOSED`, добавлен §9.0 «Примечание о нумерации дефектов», обновлены `REVIEW-REGISTRY.md` (нумерация, «Обновление 2026-09-15», D-6, D-4 реестра), память.

**Итог:** у каждой нормы `A-01…A-25`, `Q-01…Q-13` ровно **одно** определение (36/36, сверено машинно: `canonical` INDEX == фактическая строка). `validate_index.py` → **`РЕЗУЛЬТАТ: PASS — предупреждений 0`** (было 1). Дерево: 12 файлов, +81/−77.

**Уроки (важные, повторяемые):**
1. ⚠ **`read_text()` без `newline=''` уничтожает CRLF.** Первый прогон Фазы A перезаписал 7 файлов ТЗ с `\n` вместо `\r\n` — diff раздулся до 4324 строк. Диагностировано по `diff --stat` (изменился ВЕСЬ файл) и по подсчёту `\r\n` в bytes. **Правило: правки нормативного корпуса — только с `open(..., newline='')`, с проверкой `CRLF`/`LF-only` до и после.** Откат — `git checkout -- .`.
2. **Heredoc-пайпы в PowerShell ненадёжны для больших скриптов** — вывод теряется молча (правка «применилась», а `git diff` пуст). Для правок корпуса использовать файл-скрипт + `python file.py` + `echo "exit=$LASTEXITCODE"`.
3. **Каждая замена — с проверкой `count == 1`** и `sys.exit(1)` до записи файла (атомарность: запись только после успеха всех замен). Это спасло от частичных правок дважды.
4. **Порядок «инвентаризация → выбор хранителя → правка» обязателен.** Если бы я просто переименовал подзаголовки (как предполагал прежний план), норма осталась бы без определения там, где `canonical` указывал на не-определение.
5. **Проверять факт после правки, а не доверять прогнозу:** адреса, «предсказанные» в предыдущей сессии (445/311/838), разошлись с фактическими (447/313/840) на 2 строки.

**Не тронуто:** FSM, MCP-контракты, бюджетная модель, контур безопасности, схема данных, критерии Gate — изменения носят редакционно-трассировочный характер.
## Архивирование файла замечаний `SUGGESTIONS.md` (та же сессия)

**Вопрос владельца:** «файл suggestion отработан? Если да — в корзину и закоммитить».

**Аудит (по правилу Шага 0 — поиском по корпусу, не по смыслу):**
1. **Маппинг полный:** §1.1–§1.7 → `S-01…S-07`, §2.1–§2.6 → `S-08…S-13`, §3.1–§3.4 → `S-14…S-17`. 17 из 17 пунктов имеют вердикт в `REVIEW-REGISTRY.md` §1.
2. **Каждая принятая правка проверена по факту:** `S-04` (`07` §27.1), `S-06` (`02` §7 / `07` §24), `S-08` (`06` §8 / `07` §25), `S-09` (`07` §27.2 + `TASK EXEC CONTRACT` §4), `S-10` (`02` §7), `S-11` (`01` §7 / `07` §14), `S-12` (`03` §9b / `MCP TOOL CONTRACTS` §3 / `INDEX` §7 ADR-002), `S-14` (`INDEX` §6/§7), `S-15` (`07` §28.1), `S-16` (`TASK EXEC CONTRACT` §4), `S-17` (`03` §21). Снятые/отложенные (`S-01`, `S-02`, `S-03`, `S-05`, `S-07`, `S-13`) — решения зафиксированы, вносить нечего.
3. **Найдены 6 устаревших инлайн-пометок** в самом файле: `S-08`, `S-10`, `S-11`, `S-12`, `§3.4`/`S-17` — без статуса; `§3.3`/`S-16` — «частично выполнено», хотя закрыт. Файл замечаний не правился (правило `AGENTS.md` §6 — производный), факт зафиксирован в реестре §7.
4. **Найдены 2 открытых остатка:** `S-01` (инструмент сборки/обновление/подпись внутри EPIC-11) и `S-02` (preflight модели + capability-профили). Проверено поиском: `preflight`, `PyInstaller`, `подпись бинарника` в нормативных файлах **отсутствуют** — действительно не внесены. Перенесены в реестр §7 как `OPEN`, чтобы архивация их не похоронила.

**Сделано:**
- `SUGGESTIONS.md` → `корзина/SUGGESTIONS.md` (папка в `.gitignore`, в git фиксируется только удаление файла);
- `INDEX.md` шапка — в «Не включено» вместо `` `SUGGESTIONS.md` `` запись «Файлы замечаний (отработаны, архивированы в `корзина/`…)»: бэктик-ссылка на удаляемый `.md` сломала бы проверку `[1]` валидатора (`check_index_files_exist` собирает `` `([^`]+\.md)` `` по `INDEX.md`);
- `AGENTS.md` §2 — строка файлов замечаний: отмечено «Отработаны — перенесены в `корзина/`», указатель на реестр §1/§4/§5/§7;
- `ARCHITECTURE.md` стр. 189 — снят висячий указатель «см. SUGGESTIONS.md» → `INDEX.md` §7 + `REVIEW-REGISTRY.md`;
- `REVIEW-REGISTRY.md` §5 `S-05` — действие «пометить `VERIFIED / снято`» закрыто; §6 п.3 — механизм пометок уточнён под новую схему; новый **§7 «Архивирование файлов замечаний»** с итогами, проверкой по корпусу, оговоркой об устаревших инлайн-пометках и таблицей `OPEN`-остатков.

**Урок:** при архивации файла-источника главный риск — не сам файл, а (1) бэктик-ссылки на него в `INDEX.md` (валидатор их проверяет) и (2) открытые пункты, живущие только в архивируемом файле. Первое — править ссылку, второе — переносить в реестр открытой таблицей.

**Не тронуто:** ФСМ, MCP-контракты, бюджет, схема данных, критерии Gate; производные файлы (Task Cards Astra1) не правились.

## Закрытие остатков `S-01` / `S-02` — решения владельца (та же сессия, продолжение)

**Контекст:** после архивации `SUGGESTIONS.md` в реестре §7 оставались два пункта со статусом `OPEN` (перенесены туда, чтобы архивация их не похоронила). Владелец дал решения по обоим.

**Вопрос владельца был «нужно ли уточнять?» — ответ потребовал досье, а не рассуждения.** Первый ответ оказался бесполезен («я не помню всё ТЗ, твоё сообщение не даёт информации для решения»). Правильный формат — не «вот варианты», а: что уже нормировано (с адресом) → что реально осталось → что произойдёт при каждом варианте. **Урок: вопрос владельцу обязан содержать адреса норм и последствия, а не названия тем.**

**Решение по `S-01`:** сборка — **PyInstaller**; политика обновления, подпись бинарника и установщик **не вводятся** — частный portable-проект без распространения. «Убери бюрократию из ТЗ» = декларации не создавать, а не удалять существующий текст.

**Решение по `S-02`:** preflight модели и capability-профили **не вводятся** — выбор и загрузка модели относятся к зоне ответственности оператора; система не опекает пользователя. Живость бэкенда уже покрыта `Health Monitor` (`05` §2) + доменный код `LLM_UNREACHABLE` в контракте `start_research_session` (`MCP TOOL CONTRACTS` §3).

**Обоснование обоих решений (проверено по Gate):** `G-11` требует только «запускается на целевой Windows-среде без IDE + понятная диагностика отсутствующих компонентов» — ни установщика, ни автообновления, ни подписи, ни preflight модели в критериях нет. Новых норм не создано: следующий свободный ID (`A-26`/`Q-14`) **остался свободным** — это признак, что решения действительно снимающие, а не добавляющие.

**Сделано:**
- `E11-T02` (`EPIC → TASK DECOMPOSITION` §13) — `runtime/config packaging (сборка — PyInstaller)`;
- `ARCHITECTURE.md` стр. 184 — было «Сборка Windows-дистрибутива (PyInstaller/Nuitka), **инсталлятор**» → «Сборка portable-дистрибутива Windows (PyInstaller)». Это была **единственная** строка во всём репозитории с `Nuitka`/«инсталлятор» — то есть `убери` имело реальный предмет, и он был найден поиском;
- `REVIEW-REGISTRY.md` — §1 строки `S-01`/`S-02` (вердикт + целевой раздел), §5 (колонка «что остаётся к работе»), §7 (таблица остатков → решения + обоснование по границам `G-11` + оговорка «не вводится сверх существующего»).

**Проверка «не осталось ли открытого»:** поиск по `INDEX.md` (`S-01|S-02|preflight|OPEN|остаток`) — упоминаний нет, править нечего. `INDEX.md` не менялся: новых норм, сущностей и Gate нет, а реестр замечаний индексируется как реестр, не по строкам.

**Урок:** если решение владельца **снимает** пункт, а не вносит норму, — корпус почти не растёт (одна строка задачи + правка производного файла), а основная работа уходит в реестр: вердикт, обоснование, ссылка на Gate. Именно так решение не всплывёт повторно при следующем ревью.

---

## Реестр: устранено расхождение «маркер отстал от вердикта» (N-03, S-16, S-12, §6)

**Вопрос владельца:** «разберись — это решено на самом деле? Если да — исправь в документации» — про `N-03`, который в §4 реестра стоял как `⏸ DEFER — ожидание консультации с архитектором`.

**Проверка (Шаг 0 — по корпусу, не по реестру).** `N-03` действительно **закрыт** 2026-09-14: `05` §12 «Контроль доказательности» + абзац «Граница гарантий (N-03)»; `05` §14 — exact-match цитаты (стр. 332); `05` §3.2 «Egress policy — недоверенные адреса (N-03)» + журналирование нарушений (стр. 79); `07` стр. 386 — canary. `INDEX.md` §8 (стр. 257, 265) и §9 (стр. 299) — `N-03 ✅ CLOSED`. В самой ячейке §4 решение и результат уже были записаны — устарел только **ведущий** маркер.

**Исправлено (6 правок, один файл `REVIEW-REGISTRY.md`):**
- §4 `N-03` — ведущий маркер `⏸ DEFER` → `✅ RESOLVED / CLOSED`; ход решения сохранён как история;
- §4 `S-16` — ведущий `⚠ частично выполнено` → `✅ закрыто` (остальной текст ячейки уже говорил «закрыто»);
- §3 карточка `S-12` — поле «Статус в ТЗ» утверждало в настоящем времени «дефект признан открытым», хотя ТЗ обновлён (`07` §17: «Q-08 закрыт (ADR-002, 2026-09-14)») → цитата помечена исторической, добавлено текущее состояние;
- §6 п.2 — «порядок применения определяется после закрытия P0» → P0 закрыты 2026-09-14, пункты внесены;
- §6 п.4 — уточнена область действия: реестровые пометки о статусе нормативной правкой не являются и ревизию версии ТЗ не вызывают;
- §6 — **новое правило 5**: статус в §1 и в детальных разделах (§3/§4) обязан совпадать; детальная запись хранит историю, но ведущий маркер отражает текущее состояние (расхождение — класс `D-4`).

**Ложное срабатывание (важный урок).** Сначала заключил, что `S-08`/`S-10` «без статуса», и **добавил** им маркер `✅ Внесено`: маркер стоит **в середине** строки, а смотрел я лишь последние 230 символов. Дубликат снят откатом, строки идентичны `HEAD`. **Правило:** прежде чем утверждать «в строке нет X», читать строку **целиком** (`$l[$i]`, не `Substring(len-230)`) либо считать вхождения маркера по всей строке; эмодзи в PowerShell-регексе задавать кодами (`[string][char]0x2705`).

**Валидация:** `РЕЗУЛЬТАТ: PASS — предупреждений 0` (все шесть проверок), переводы строк LF, BOM отсутствует.

