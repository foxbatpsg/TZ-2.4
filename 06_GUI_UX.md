
# Research Prompt Suite — GUI и UX v2.4

**Статус:** нормативная спецификация

---

## 1. Technology

Windows GUI.

**Базовый стек:**

- Python;
- CustomTkinter;
- SQLite.

Браузер не требуется для работы приложения.

---

## 2. Main Thread / Worker

**Main Thread:**

- GUI;
- events;
- rendering.

**Worker Thread:**

- постоянный asyncio loop;
- network;
- orchestration;
- MCP-related operations.

**Взаимодействие:**

- `asyncio.run_coroutine_threadsafe()`;
- thread-safe queue;
- `root.after()`.

---

## 3. Study creation

GUI позволяет:

- создать Project;
- создать Study;
- ввести question/goal;
- выбрать research mode;
- выбрать source scope;
- задать filters/exclusions;
- подключить Project/архив;
- добавить local documents;
- просмотреть план;
- изменить запросы;
- запустить/остановить исследование.

---

## 4. Intent Blueprint Editor

Показывает:

- task;
- purpose;
- query;
- source;
- filters;
- exclusions;
- expected material type;
- priority;
- estimated budget.

Пользователь может:

- изменить query;
- отключить task;
- изменить priority;
- изменить source;
- сохранить вариант.

---

## 5. X-Ray Activity Stream

Показывает человекочитаемые события:

- поиск;
- ranking;
- fetch;
- parsing;
- chunking;
- Evidence extraction;
- gap detection;
- ошибки.

Логи передаются через `queue.Queue`.

UI обновляет их батчами через `root.after()`.

Циклический буфер: последние 150–300 строк, значение конфигурируемое.

---

## 6. Evidence Explorer

Клик по Evidence открывает side panel:

```
Claim
 ↓
Evidence
 ↓
Document
 ↓
Source
```

Показываются:

- точный текст Evidence;
- location;
- source;
- document metadata.

---

## 7. Evidence Tree

```
Claim
 ├── Evidence
 │    └── Source
 ├── Evidence
 │    └── Source
 └── Contradiction
```

Цветовая индикация является только визуальным отображением статуса, а не отдельной логикой оценки.

---

## 8. Report Viewer

Smart scroll:

- если пользователь читает выше конца, auto-scroll отключается;
- отображается кнопка перехода к концу.

---

## 9. Graceful Abort

### Soft Stop

- новые сетевые запросы не запускаются;
- текущие безопасно завершаются;
- разрешается финальный synthesis;
- создается partial report.

### Hard Stop

- новые операции запрещаются;
- asyncio tasks отменяются;
- cancellation является кооперативной;
- активные CPU threads не считаются мгновенно уничтоженными;
- состояние фиксируется checkpoint'ом;
- SQLite остается консистентной;
- после остановки можно сформировать partial report.

### Кооперативная отмена CPU-задач

Операции, выполняемые в CPU-потоках (чанкинг, индексация, стемминг, массовая обработка), не могут быть прерваны мгновенно через отмену асинхронных задач. Для них реализуется механизм кооперативной отмены:

1. **Флаг остановки:** Каждой долгой операции передаётся флаг остановки (через механизм `threading.Event`).
2. **Периодическая проверка:** Долгие циклы обязаны проверять флаг на каждой итерации или после каждого батча.
3. **Soft Stop:** Флаг устанавливается, операция завершается после текущей итерации, состояние сохраняется.
4. **Hard Stop:** Флаг устанавливается, если операция не завершилась в течение заданного таймаута (например, 5 секунд), поток принудительно прерывается с фиксацией checkpoint.

**Требования к точкам проверки:**

- Чанкинг: проверка флага после каждого чанка.
- Индексация: проверка флага после каждого батча чанков.
- Стемминг: проверка флага после каждого документа.
- Перестроение индекса: проверка флага после каждого батча.

**При любом типе остановки:**

- Уже обработанные данные сохраняются.
- Консистентность гарантируется.
- Фиксируется контрольная точка.

---

## 10. System Health Monitor

Показывает:

1. LLM status;
2. backend;
3. queue size;
4. active tasks;
5. DB size;
6. disk free space;
7. network/source errors.

контроль базы данных:
1. Размер текущей проектной БД, 
2. размер реестра, 
3. свободное место на диске.

---

## 11. Prompt Sandbox

Позволяет:

- выбрать Evidence;
- применить prompt;
- получить пробный результат;
- не изменять Study.

---

## 12. Research Snapshots

Snapshot содержит:

- ResearchState;
- Claims;
- Evidence references;
- Working Memory;
- task state.

Откат не должен разрушать глобальный Document Repository.

---

## 13. Projects

GUI поддерживает:

- create;
- rename;
- archive;
- delete with confirmation;
- transfer Study.

При новом Study можно выбрать:

- текущий Project;
- отдельные Studies;
- отдельные documents;
- весь локальный архив.

Переключение проектов = переключение активной БД. GUI отображает список проектов из реестра. Создание проекта = создание нового .db файла + записи в реестре. Архивация = смена статуса в реестре, файл сохраняется на диске.

---

## 14. Offline mode

При отсутствии сети пользователь может:

- искать по локальным материалам;
- просматривать Evidence;
- анализировать Study;
- создавать report;
- экспортировать результат.

---

## 15. Review / Aggregation UI

Для aggregation-задач GUI должен показывать:

- число обработанных отзывов/наблюдений;
- число уникальных источников;
- основные аспекты;
- положительные/отрицательные паттерны;
- распределение наблюдений;
- аномальные группы;
- ограничения выборки.

---

## 16. Query preview

Перед запуском пользователь видит все SearchTask.

Запуск исследования без обязательного preview допускается только после явной настройки соответствующего режима пользователем.

---

**Конец Раздела 6.**
